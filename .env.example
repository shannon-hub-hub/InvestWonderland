DATABASE_URL=postgresql+psycopg://postgres:password@localhost:5433/scraper
# Optional: real AI verdicts (otherwise template synthesis)
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
FEED_LOOKBACK_DAYS=30
INGEST_MAX_PAGES=50
STARTUPS_GALLERY_URL=https://startups.gallery/categories/stages/series-c
# Docker scheduler service runs daily ingest at 06:00 UTC (see crontab)
# K8s: kubectl apply -f deploy/k8s/cronjob-ingest.yaml
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
